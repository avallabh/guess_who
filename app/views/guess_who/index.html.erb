<h1>INDEX PAGE</h1>

<!-- Check for current user fails -->
<% if user.uid.exists? %>
 Logged in as <%= current_user.name %> <%= current_user.nickname %> <%= current_user.email %>
 <%= link_to 'Sign Out', logout_path %>
<% else %>
  <%= link_to 'Sign In With GitHub', '/auth/github' %>
<% end %>
